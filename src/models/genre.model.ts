// import connection from './db.model.ts';

// constructor
const Property = function(property: any) { //TODO define the interface/types and substitute here 
    property.name;
    property.description;
    property.type;
    property.size;
    property.price;
    property.location;
    property.constructed_date;
    property.image;
};